name: flutter_voice_record_play
description: "A new Flutter project."

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  cupertino_icons: ^1.0.6
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  just_audio: ^0.9.36
  path_provider: ^2.1.1
  record: ^5.0.4

dev_dependencies:
  build_runner: ^2.4.11
  flutter_lints: ^3.0.0
  hive_generator: ^2.0.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
